import 'package:flutter/material.dart';

class EmailEditPage extends StatelessWidget {
  const EmailEditPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('EmailEditPage')),
      body: const Center(child: Text('EmailEditPage')),
    );
  }
}