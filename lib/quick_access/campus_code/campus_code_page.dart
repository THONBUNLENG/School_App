import 'package:flutter/material.dart';

class CampusCodePage extends StatelessWidget {
  const CampusCodePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('CampusCodePage')),
      body: const Center(child: Text('CampusCodePage')),
    );
  }
}