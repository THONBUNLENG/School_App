import 'package:flutter/material.dart';

class NetTopUpPage extends StatelessWidget {
  const NetTopUpPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Net TopUp ')),
      body: const Center(child: Text('NetTopUp Page')),
    );
  }
}