import 'package:flutter/material.dart';

class NetTransferPage extends StatelessWidget {
  const NetTransferPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Net Transfer')),
      body: const Center(child: Text('Net Transfer Page')),
    );
  }
}